# Install: pip install -r requirements.txt
# FFmpeg also required: winget install ffmpeg

requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
polygon-api-client>=1.12.0  # Massive (formerly Polygon.io) — package name unchanged
notebook>=7.0.0
ipykernel>=6.0.0
python-dotenv>=1.0.0
pyyaml>=6.0    # committee membership (unitedstates/congress-legislators)
scipy>=1.11.0      # audio_engine — filters/resampling
soundfile>=0.12.0  # audio_engine — WAV I/O
